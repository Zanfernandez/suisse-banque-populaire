services:
  - type: static
    name: suisse-banque-populaire
    staticPublishPath: ./
    buildCommand: "echo 'Static site - no build needed'"
    routes:
      - path: "/*"
        destination: "/index.html"
